<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="bg-zinc-900 w-full min-h-screen p-10 text-white">
        <div class="w-full flex justify-end">
       <a class="bg-red-500 rounded-md px-2 py-1 text-sm inline-block mb-5" href="/logout">Logout</a>
       </div>
       <div class="flex items-start gap-3">
                <div class=" w-10 h-10 bg-red-500">
                    <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" />
                </div>
               <h1 class=" text-2xl">Hello , <%= user.name %> 👋</h1>
       </div>
         
                <h5 class=" mb-3 mt-3">you can create a new post.</h5>
      
       
        
       <form action="/post" method="post" >
        <textarea placeholder="What's on your mind ?" class="resize-none w-1/3 bg-zinc-700 rounded-md border-2 border-zinc-700 px-2 py-1 block mb-3" name="content" ></textarea>
         <input class="bg-blue-500 rounded-md px-3 py-2 " type="submit" value="Create New Post">
       </form>
       
        <div class=" mt-20  text-white">
            <h3 class="text-white text-xl mb-3">Your Posts.</h3>
            <div class="postContainer ">
              
                <% if (user.posts.length > 0) { %>
  <% user.posts.reverse().forEach(function (val) { %>
    <div class="post p-4 bg-zinc-800 rounded-md border border-zinc-700 w-1/3 mb-4">
      <h4 class="text-blue-500 mb-2">@Async</h4>
      <p class="tracking-tighter mb-2"><%= val.content %></p>
      <small><%= val.Likes.length %></small>
      <div class="flex gap-3">
        <a class="text-blue-500"   href="/like/<%= val._id %>">Like</a>
        <a class="text-zinc-700 " href="/edit/<%= val._id %>">Edit</a>
      </div>
    </div>
  <% }) %>
<% } else { %>
  <p class="text-white">No posts yet.</p>
<% } %>

            </div>
        </div>
</div>
</body>
</html>