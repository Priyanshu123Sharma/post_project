<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="bg-zinc-900 w-full min-h-screen p-10 text-white">
        <div class="w-full flex justify-end">
       <a class="bg-red-500 rounded-md px-2 py-1 text-sm inline-block mb-5" href="/logout">Logout</a>
       </div>
        <h5 class=" mb-3"> Edit your post</h5>
       <form action="/update/<%= post._id %>" method="post" >
        <textarea placeholder="What's on your mind ?" class="resize-none w-1/3 bg-zinc-700 rounded-md border-2 border-zinc-700 px-2 py-1 block mb-3" name="content" > <%= post.content %></textarea>
         <input class="bg-blue-500 rounded-md px-3 py-2 " type="submit" value="Edit Post">
       </form>
       
       
</div>
</body>
</html>